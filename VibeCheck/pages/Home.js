import { Button, View } from "native-base";
import React from "react";

const Home = () => {
    return (
        <View>
            <Button>
            </Button>
        </View>
    );
}

export default Home;